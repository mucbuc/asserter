{
	"scripts": {
		"test": "tape test/test.js",
                "format": "clang-format -i --style=Webkit src/*.h src/*.cpp test/src/*.cpp"
	},
	"dependencies": {
		"expector": "*",
		"tape": "^4.13.0"
	}
}
